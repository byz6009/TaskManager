# 第 0 轮

```text
这是软件工程课堂实践，请协助我分轮开发任务管理应用。先只规划，不写代码。

课件要求：
1. 任务增删改查，标题必填，描述选填。
2. 状态为待办、进行中、完成。
3. 优先级高/中/低，分别用红/黄/绿表示。
4. 三列看板，卡片拖拽到另一列后改变状态。
5. 一键切换深浅主题，刷新后记住选择。
6. 使用浏览器 localStorage，刷新后任务不丢失。

技术栈：Vue 3 + Vite + Tailwind CSS 4 + JavaScript，纯前端。
本机课程工具在：
C:\Users\Administrator\Documents\Codex\2026-09-22\agent-x20\outputs\environment\Enter-CourseEnv.ps1
需要运行 Node/npm 时，在同一个 PowerShell 命令块先点加载该脚本。
使用 npm.cmd，不要假设普通终端已配置 npm。

请先只读检查目录与现有文件，不执行安装、写文件或提交。
请给出：任务单、需求验收表、简短目录结构、任务数据结构、分轮计划。
任务单写清目标、范围、上下文、验收、约束和停止条件。
如果目录为空，基线写为“尚无代码和测试”，不要编造已有测试通过。
接口采用教程第 19 节的建议契约；若需要简化，请先指出差异，统一后再开发。
业务纯函数与 Vue 组件分离，让 Node 内置测试能够直接验证关键行为。
结构保持简单，优先使用 Vue 自带状态管理及桌面原生拖拽。
每轮按“计划、执行、浏览器验收、Git 提交”完成。
每轮先用几句话说明计划，再完成本轮实现和构建，给出验收方法后停止。
我会反馈浏览器结果，确认后再提交并进入下一轮。
每轮返回实际修改文件、相关测试/构建结果、diff 摘要、尚未验证项及下一步。
真实记录执行过的命令和结果，不能把未执行的浏览器测试写成通过。
工程边界：可使用已配置环境和约定依赖；不新增后端、不发布、不推送远端。
如果要改变数据契约、引入额外依赖或改动范围外文件，先解释必要性和影响。
相同问题连续两次没有新证据时，整理失败日志和需要的信息，避免盲目重试。
目前只输出计划。
```
</br>
</br>

# 第 1 轮
```text
计划确认。开始第 1 轮，只建立项目骨架。

先确认当前目录是我选择的 course-task-board，并检查已有文件。
若为空，直接建立最小的 Vue + JavaScript + Vite 项目文件，不额外下载项目生成器。
至少包含 index.html、package.json、vite.config.js、src/main.js、src/App.vue、src/style.css。
package.json 使用 type: module，scripts 包含 dev: vite、build: vite build、test: node --test。
使用本机课程环境，固定以下依赖版本：
vue 3.5.43
vite 8.3.0
@vitejs/plugin-vue 6.0.9
tailwindcss 4.3.3
@tailwindcss/vite 4.3.3

package.json 中以上依赖使用精确版本，先运行 npm.cmd install --offline --no-audit --no-fund，复用课程缓存。
如果报告缺少缓存，记录具体缺少项，再运行 npm.cmd install --prefer-offline --no-audit --no-fund 补齐。

接通 Vue 和 Tailwind 的 Vite 插件，在入口 CSS 导入 Tailwind。
页面先显示中文标题“任务管理”，加一个使用 Tailwind 的样式元素证明样式生效。
删除模板无关展示内容，保持结构简单。此轮先不实现 CRUD、存储和拖拽。
在项目根目录初始化独立 Git 仓库，检查 .gitignore 包含 node_modules 和 dist。
创建项目 AGENTS.md，保存技术栈、分轮工作方式、构建命令及真实记录原则。
将第 0 轮确认结果保存为 docs/task-brief.md 和 docs/contract.md。
创建 docs/iteration-log.md，先记录本轮目标、实际改动、实际命令和待人工验收项。
建立 src/domain、src/storage、tests 目录的规划，实际文件按功能轮次添加。
此时若没有测试用例，就记录“测试尚未建立”，不能把零用例当成测试覆盖通过。
运行 npm.cmd run build，启动开发服务器到固定的 http://localhost:5173。
端口占用时先报告，不要静默切换端口。
列出我需要在浏览器观察的现象，等我反馈后再提交。
```
</br>
</br>

# 第 2 轮
```text
开始第 2 轮：任务列表和 CRUD。
先读取项目 AGENTS.md，简述计划，再完成本轮开发。

验收标准：
1. 新建任务：标题 trim 后不能为空，描述可留空。
2. 能看到任务标题、描述、状态和优先级。
3. 能编辑同一任务，保存后不新增重复卡片；取消编辑不改变原任务。
4. 能删除指定任务，不影响其他任务。
5. 状态为 todo/doing/done，对应待办/进行中/完成。
6. 优先级为 high/medium/low，对应高红、中黄、低绿，颜色旁有文字。
7. 使用稳定且唯一的 id，列表 key 不使用数组位置。
8. 默认状态待办，默认优先级中。空列表有明确提示。

本轮先用内存数据，持久化下一轮再加。
按已确认契约在 src/domain/taskRules.js 实现 validateTaskInput。
把标题和枚举校验集中到此纯函数，并在新建、编辑两条路径复用。
在 tests/taskRules.test.js 编写有实际断言的 Node 内置测试，至少覆盖纯空格标题、可空描述和非法状态/优先级。
按职责拆分必要组件，保持实现适合课堂讲解。
执行相关测试、npm.cmd test 和 build，检查 diff 是否只涉及本轮内容。
更新日志，列出浏览器检查步骤，等我验收后再提交。
```
</br>
</br>

# 第 3 轮
```text
开始第 3 轮：用 localStorage 持久化任务。
保留第 2 轮行为，简述计划后实现。

要求：
1. 存储键统一使用 course-task-board.tasks.v1，JSON 存储。
2. 启动先恢复数据，再监听和保存修改，不能先把空数组覆盖到存储中。
3. 新建、编辑、删除、状态变更、优先级变更都触发保存。
4. 正确保存空数组：删除最后一个任务后刷新，仍然没有任务。
5. 读取 JSON 或写入存储失败时，页面不能白屏，应给出简短说明。
6. 不要在每次刷新时重新插入示例任务，不使用 localStorage.clear()。
7. 在 src/storage/taskStorage.js 使用第 19 节的纯解析/序列化接口，让测试不依赖 window。
8. tests/taskStorage.test.js 至少覆盖无数据、空数组、正常往返和损坏数据；损坏数据不能被静默覆盖。

运行存储定向测试、全部已有测试和 build，确保标题校验等旧行为仍通过。
给出新建、编辑、删除后分别刷新页面的检查步骤。
更新日志，等我验收，再提交。
```
</br>
</br>

# 第 4 轮
```text
开始第 4 轮：三列看板与拖拽改状态。

显示待办、进行中、完成三列；每列有任务数量，空列也能接收拖拽。
如果保留列表视图，两种视图必须共用同一份任务数据。
优先使用桌面浏览器原生 HTML Drag and Drop，不额外安装拖拽库。
卡片 dragstart 携带任务 id，目标列 dragover 阻止默认行为，drop 按 id 修改状态。
拖拽只改变该任务状态，不复制、不删除任务；原地放下无副作用。
状态变更通过已有的 localStorage 保存逻辑持久化。
提供清晰的可放置提示，拖拽结束后清除视觉状态。
卡片上的编辑和删除按钮继续可用，保留表单/选择器修改状态的方式。
按契约在 src/domain/taskRules.js 增加 moveTask，并在真实 drop 处理里调用。
增加测试：跨列移动后数量和 id 集合不变、其他任务内容不变；无效 id/状态不改原数据。

运行相关测试、全部已有测试和 build，更新日志。
列出跨列拖拽、空列接收、重复拖拽、刷新恢复的验证步骤。
等我验收后再提交。
```
</br>
</br>

# 第 5 轮
```text
开始第 5 轮：深浅主题切换和必要的界面收尾。

提供一键切换按钮，用 course-task-board.theme.v1 保存 light 或 dark。
有保存选择时优先恢复该选择；第一次打开可默认浅色。
使用 Tailwind 4 的 class 驱动 dark 变体，切换根元素的 dark 类。
检查页面背景、文字、卡片、表单、弹窗和按钮在两种主题下都可读。
高/中/低优先级的红/黄/绿语义不变，仍有中文标签。
长标题不挤坏卡片；列表为空时有说明；窗口较窄时布局仍能操作。
不要加入本轮之外的新功能。

运行全部已有测试和 build，检查本轮 diff，确认任务规则未被改坏。
更新日志，给出浅→深→刷新、深→浅→刷新的验收步骤。
等我验收后再提交。
```
</br>
</br>

# 第 6 轮
```text
进入最终验收。先对照课件 R1–R6 检查已有实现，不扩展功能。
运行 npm.cmd test 和 npm.cmd run build，检查 Git 状态、最近提交及本轮 diff。
核对测试实际发现并执行了用例，零用例或全部 skipped 不能算已覆盖。
若项目已有 lint/typecheck，运行已有命令；本教程未配置时标记“未配置”，不要伪报通过。
对照下面的验收表记录结果，区分“命令已验证”“用户人工已验证”“尚未验证”。
没有浏览器操作能力时，明确给出人工步骤，不得编造浏览器测试或截图。
若发现具体失败，先修复该问题，并复测相关功能。

整理 README：项目用途、技术栈、安装启动构建命令、存储方式、已知限制。
整理 docs/iteration-log.md：每轮提示词摘要、实际改动、验证情况、对应提交。
整理 docs/review.md：问题、修改原因、证据、尚未覆盖范围、审查重点和回滚方法。
做一遍只读审查，再根据具体发现做最小修复；不要把审查变成新增功能。
根据真实代码和真实记录给出 5–10 分钟汇报提纲。
不得编造开发时间、遇到过的 Bug、测试数量或不存在的 Git 提交。
告诉我还缺哪些验收证据，等待我最后确认后提交文档与修复。
```